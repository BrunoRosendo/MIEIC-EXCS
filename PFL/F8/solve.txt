1-
a) X = 1? no
   X = 2? no
   No
b) X=1, Y=1 ? no
   X=1, Y=2 ? no
   X=2, Y=1 ? no
   X=2, Y=2 ? no
   No

c) X=1, Y=1 ? no
   X=1, Y=2 ? no
   No (X nao volta a ser tentado)

2-
a) one
   two
   three
   five

b) one
   (ele nao passa para tras e tambem nao passa para baixo)

c) one-one
   one-two
   one-three

3-
vermelho (é a definição de not)
verde (X é so de uma especie, nao faz diferença, é so eficiencia) - comparado a switch case
